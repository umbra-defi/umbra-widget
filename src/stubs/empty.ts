// Empty stub for optional native deps the client-platform/web barrel
// references but the widget never uses (hardware-wallet transports). Aliased
// in vite.playground.config.ts so the dev bundler doesn't try to resolve them.
export default {}
